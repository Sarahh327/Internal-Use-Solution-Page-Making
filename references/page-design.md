# 页面视觉与交付

来源：用户在 EMS 官网落地稿中的明确选择。是后续方案页的默认基线，可被新的用户要求覆盖。

## 页面框架

- 顶部白底导航、细底边。左侧官方黑色 logo；中间 Product / Solution / Docs / Resource / Pricing，随后 Discord、GitHub 图标；右侧免费试用（英文 Free Trial）、绿色 LOG IN、语言入口。
- 使用真实可验证的官网链接，优先复用项目已有公共 header/footer。不要复制旧稿里临时指向首页的占位链接。未知链接记录在交付说明中，不制造可点击的假功能。
- 第一屏直接进入 Hero：桌面左标题、右可点击产品模型，移动端合理堆叠。主标题说明具体应用和场景。不要再加 Home / Solution / Energy Management 或 Energy management · EMS。
- Hero 与适当关键节点统一放「免费试用 / 联系团队」，英文为「Free Trial / Book a Demo」。导航与页脚中的同类 CTA 同样使用该文案；登录入口保留 LOG IN。避免每段一个 CTA。
- 白底、大留白、清楚层级、适中字体粗细。使用 design skill 的官方资产和 token；禁止用文字、CSS、AI 重画 logo。Tier0 可作为正文产品名出现。

## 主标题措辞与换行

- 主标题默认突出“构建 100% 适配业务、支持灵活变更的 [具体系统名称]”，随应用调整措辞；副文用页面、字段、权限和流程等可配置项支撑，不追加空泛口号。“100%”表达定制适配方向，不延伸为自动满足所有需求、零实施或效果保证。
- 中文 H1 在自身文本容器内自然换行、两端对齐，末行左对齐。默认使用 `text-align: justify; text-align-last: left; text-justify: inter-ideograph;`；容器允许收缩（如 `min-width: 0; max-width: 100%`），保持正常字距。
- 不用硬编码 `<br>`、空格填充、固定高度或大字距凑行；数字与百分号（如 `100%`）可局部 `white-space: nowrap`，不能禁止整个标题换行。强调文字默认行内展示，避免强制另起一行。
- 英文按英文语序简练改写，默认左对齐、自然换行，避免照搬中文两端对齐导致大词间距。检查桌面、窄屏和移动端的溢出、孤字与标题/图片比例；浏览器不可用时如实记录未验证，不把 CSS 检查当视觉通过。

## 页脚

- 近黑底，左侧官方白色 logo。其下订阅区文字：STAY UP TO DATE WITH PRODUCT UPDATES, LEARNING RESOURCES, AND MORE.
- 商务邮箱输入框配 Subscribe；其下 Join Discord、联系团队（英文 Book a Demo）和 LinkedIn / GitHub / Facebook / X / YouTube 社交入口，底部 Privacy Policy。
- 竖向分隔后，右侧三列目录：Tier0、Solutions、Resources。
- Tier0 作为统一产品，页脚按应用构建、Unified Namespace、App Library、Advanced Analytics、Data Collection 等功能组织入口，不拆分 Tier0 Builder 与 Tier0 Platform 产品。
- Solutions 列按 By Use Case / By Capability 分组，并有 Docs；Resources 列列 Blogs / Events / Downloads，并有 Pricing。具体目录跟随已核实官网结构，不把旧页面名称当永久清单。
- 移动端可堆叠或折叠，文字、输入与按钮可操作。预览订阅未接后台时不能假报订阅成功；使用清楚的预览行为并在交付说明中记录。

## 视觉证据

- 原始截图保留原文件；裁去浏览器栏、通知、无关项目名和系统截图小缩略图等噪声，不编造产品结果。只有相关截图才能用作功能证据。
- UNS 示例用可编辑树：Factory / Workshop / Line / Equipment，节点下 Metric / State；结合该方案所需单位、时间和对象。它是模拟数据模型，不声称实际项目已连通。
- Builder 与 UNS 的真实界面以 [产品界面参考](product-ui.md) 中的用户截图为基线；默认按原图制作可点击 HTML 模型；只在明确保留原始证据时直接用图。Builder 的提示词与项目名贴合当前方案。Agent 用简短问题、数据表或图、可执行下一步展示。原生 HTML/SVG 用于图形与文字；栅格图片仅用于品牌、真实照片与明确保留的原始证据，不充当演示 UI。
- 保留语义正文、表格标题和必要可访问文本。不要为了视觉简洁把关键语义只藏在 alt 或 aria-label 中。
- 尽量一个模块一个问题。没有内容增量就删掉副标题、小字和重复引导。
- 流程用步骤与箭头，层级用树，关联用关系图，差异用简表；选最能解释当前问题的形式，不把长段落塞进框里冒充图形。图形已说明的信息不再逐项复述，短文只补结论与必要前提。

## 双语模型与证据本地化

- 首屏为原始产品截图，普通说明为静态图文，后续产品界面采用交互模型。中英文都保持该分工，并翻译产品演示全部可见状态。
- 保留品牌、编码、单号、数量、日期、单位与业务状态，统一 UI 术语。不能因本地化增加产品能力、修改业务结果或虚构客户数据。
- 原始截图保留并记录对应关系，以真实界面为视觉依据重建可操作 HTML。模型本地化不证明产品本身已支持目标语言；将依据与未验证项写入交接资料。
- 用户明确要求保留的截图另存本地化衍生图，逐张核对译文、数值、清晰度和布局；不能以修改 alt 或放大原图代替翻译。没有采用的附件无需转换。
- 逐语言实际操作模型、切换语言与重新打开交付文件。不能只对首态截图做视觉核对，就声称所有交互状态已翻译。

## 设计交付

- 收到软件截图与业务描述并请求制作方案时，直接制作可打开的中文响应式 HTML，附必要的素材和 SEO 说明；无需用户再说一次“交付 HTML”。复用现有技术栈，不擅自换平台。明确只要文案或其他格式时按用户要求执行。
- 迭代阶段持续修改同一中文版基线，保留已确认结构、文案和组件；不提前做英文译图，也不每次自动打包。只有中文稿时，语言入口不可链接到不存在的英文页。
- 用户要求双语时，以当时最新中文版生成英文正文、图形、Prompt、Agent 问答、图片和元数据，避免翻译旧版。用户要求“打包”默认指中英文 HTML、可编辑交互组件及各自使用的证据图片，同时授权完成模型、全部交互状态和证据的本地化并打包；用户明确只要单语时例外，不再追加“是否翻译图片”的确认。
- 双语包包含可打开的中英文 HTML、全部必要资源及简短使用说明；交互模型及其数据、脚本必须随包；另存 HTML 实际使用的中文和英文证据图片，分别放入 `screenshots/zh-CN/` 与 `screenshots/en/`，附对应清单，没有采用静态证据时不必额外制造截图；内嵌证据仍保留原文件；语言互链可用，解压后不依赖原工作目录。导出前检查两种语言的业务内容、截图版本与模块一致，运行静态检查并记录未验证项。仅要求双语页面时不擅自打 ZIP；上传 GitHub 或发布仍需对应指令。
- 双语默认独立可链接 URL，语言切换到对应页面；图片遵守上述本地化规则，不将译制图称为真实产品原始截图。
- 向设计团队交付时包含：中英稿、源 HTML/组件、真实原图、官方 logo、字体及许可（允许分发时）、素材映射、未接入功能和待核实链接说明。
- 核实实际像素和导出尺寸。放大图片明确叫放大版；要清楚文字优先矢量排版加原图，不反复让图像模型重绘产品小字。

- 交互模型下的补充正文与所属模块对齐，使用正常正文字号和自然换行；避免额外窄宽度造成意外断行。Namespace 与 Agent 均以单个紧凑版块展示，优先删重复内容，不能靠持续缩小字号解决高度。

## 底部服务模式固定模块

底部服务模式按 [EMS 固定参考](service-modes.md) 复用结构与样式，按应用替换专业内容、按页面顺序更新编号，按钮统一中文「免费试用 / 联系团队」、英文「Free Trial / Book a Demo」。

仅产品界面演示遵守 [交互式界面模型](interactive-demos.md)；普通说明静态。自包含 HTML 在可用浏览器中验证离线交互，未验证时如实说明。

## 首屏与编号
首屏桌面左侧标题、说明与 CTA，右侧用户原始产品截图；移动端上下排列。保持图片自然比例，不拉伸、不裁去关键信息，不用低完成度交互模型替换原图。首屏无栏目标签；第二个正文板块从 01 连续编号，使用统一「编号 / 英文栏目名」，子卡片不重复编号。
